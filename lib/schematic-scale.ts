// Altium's smallest junction has radius 2. This scale gives it the same
// 0.03-unit radius as Circuit JSON without modifying its native SIZE enum.
export const DEFAULT_SCHEMATIC_UNITS_PER_CIRCUIT_UNIT = 200 / 3
